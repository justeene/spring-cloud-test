spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: "jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false"
    username: "root"
    password: "123456"
    min-idle: 10
    max-active: 100
    max-idle: 10